#include <iostream>
#include "BitWise.h"

using namespace std; 

/*int main () {
	BitWise b;
	b.set_value(2);
	cout << "value: " << b.getValue();


	b.set_value(b.shift_left_by(2));
	cout << "shifted value: " << b.getValue();

	int age; 
	cin >> age;
	return 0;
}*/
